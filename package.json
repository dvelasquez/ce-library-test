{
  "name": "@dvelasquez/root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "lerna exec -- rollup -c=../../rollup.config.js",
    "watch": "lerna exec --no-sort -- rollup -w -c=../../rollup.config.js",
    "lerna:bootstrap": "lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^3.16.4",
    "rollup": "^1.21.4",
    "rollup-plugin-typescript2": "^0.24.2",
    "typescript": "^3.0.1"
  },
  "dependencies": {
    "@dvelasquez/ce-property": "*"
  },
  "workspaces": [
    "packages/*"
  ]
}
